<!DOCTYPE html>
<html lang="es">
  <head>
    <title><%= title %></title>
    <meta charset="utf-8" />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <h1>Portal Pacientes</h1>
    <p>Bienvenido al portal de pacientes</p>
    <ul>if (pacientes.length === 0{{ %>
      <li>No hay pacientes registrados.</li><% 
    } else { %>
     <% pacientes.forEach(p => { %>
        <li><%= p.nombre %> - <%= p.edad %> años</li>
      <% }); %>}
    <% }) %>
    </ul>
    <footer>
      <p>© <%= new Date().getFullYear() %> Mi Aplicación</p>
    </footer>
  </body>
</html>